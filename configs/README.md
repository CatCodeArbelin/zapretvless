# Configs

Каталог содержит только безопасные bootstrap-примеры конфигурации без реальных ключей, VLESS URI, hostnames, tokens или production-значений.

`example.bootstrap.json` показывает, что runtime не реализован, а сетевые изменения отключены.

PR-01 — только bootstrap. PR-02 ещё не делался; VLESS parser, XrayEngine и ZapretEngine не реализованы. Xray/Zapret/WinDivert не запускаются, DNS/proxy/routes не меняются, PR и merge делает человек вручную.
